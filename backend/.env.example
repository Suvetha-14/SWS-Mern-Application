# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb://127.0.0.1:27017/document_management

# Client Origin (CORS)
CLIENT_URL=http://localhost:5173

# Upload Configuration
MAX_FILE_SIZE_MB=10

# AI Provider Configuration (Optional - System defaults to smart local mock/fallback engine when empty)
GEMINI_API_KEY=
OPENAI_API_KEY=
